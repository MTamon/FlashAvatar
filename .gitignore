dataset
metrical-tracker
**/__pycache__
*.egg-info/
build/
*.so
*avi
*mp4
*jpg
*ply
*.txt
*.sh
flame/generic_model.pkl
flame/FLAME_masks
flame/female_model.pkl
flame/male_model.pkl
flame/FLAME_masks.zip

# Cloned external tooling (e.g. metrical-tracker). Kept outside git.
external/

# Explicit un-ignores (files tracked in the repo even though the patterns above
# would otherwise exclude them).
!requirements_128.txt
!install_128.sh
!scripts/*.sh
!README.md

example.zip
*.zip
*.pth
*.pt